logging:
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS
spring:
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: root
    password: password
    virtual-host: /
    listener:
      simple:
        prefetch: 1 # 只能預先取一個